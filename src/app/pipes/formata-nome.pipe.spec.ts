import { FormataNomePipe } from './formata-nome.pipe';

describe('FormataNomePipe', () => {
  it('create an instance', () => {
    const pipe = new FormataNomePipe();
    expect(pipe).toBeTruthy();
  });
});
