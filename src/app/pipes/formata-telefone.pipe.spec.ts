import { FormataTelefonePipe } from './formata-telefone.pipe';

describe('FormataTelefonePipe', () => {
  it('create an instance', () => {
    const pipe = new FormataTelefonePipe();
    expect(pipe).toBeTruthy();
  });
});
